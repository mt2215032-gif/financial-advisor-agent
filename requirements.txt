langchain>=1.3,<2.0
langchain-core>=1.6,<2.0
streamlit>=1.62,<2.0
pandas>=2.2
python-dotenv>=1.0

# --- Providers --------------------------------------------------------------
# Claude is the default (LLM_PROVIDER=anthropic). The others are installed
# here too so the provider picker in the UI works out of the box; drop the
# lines you do not need.
langchain-anthropic>=1.7,<2.0
langchain-openai>=1.6,<2.0
langchain-ollama>=1.1,<2.0
